# pytest configuration for MedLearn Agent
# Registers custom marks to suppress PytestUnknownMarkWarning

[pytest]
markers =
    e2e: Tests that require the running FastAPI server
    standalone: Tests that call run_graph() directly (no server needed)

# Default options
addopts = -v --tb=short
testpaths = tests
